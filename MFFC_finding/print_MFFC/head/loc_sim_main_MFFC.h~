#ifndef LOC_SIM_MAIN_MFFC_H
#define LOC_SIM_MAIN_MffC_H

#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <cstdlib>

void loc_sim_main_MFFC(BnetNetwork *net, string &filename, double threshold);
int loc_sim_ite_MFFC(BnetNetwork *net, BnetNetwork *&net_comb, DdManager **dd_comb, map<string, struct score_pla> &sim_record, char *last_node, vector<char*> &last_inputs, double threshold, double &real_er, int &iIndex);

#endif
